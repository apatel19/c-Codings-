#include <iostream>
#include <string>
#include <cstdlib>
#include <vector>

using namespace std;

/*void sortThis (string s, int size) {

vector<string> left;
vector<string>right;
int mid = size / 2;
int i = 0, j = size; 
while (i < mid){
//left[i] = s[i];
left.push_back(s[i]);
i++;
}
j = 0;
while (mid < size) {
//right[j] = s[mid];
right.push_back(s[mid]);
mid++;
}

cout << "left: " << left << endl;
cout << "right: " << right << endl;

}*/

int main (int argc, char **argv ) {

vector <string> s1;
s1.push_back("Indravadam");
s1.push_back("Patel");
s1.push_back("Apurv");
int i, j;

//sortThis (s1.at(0), s1.at(0).size());

for (i = 100; i >= 0; --i) {

cout << i ;
	//for (j = 0; i < s1.at(i).size(); i++) {
	
	
//}

}

return 0;

}
